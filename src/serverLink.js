// holding here the main localhost backend server link

// For offline local server enable this below comment and comment the other one
// const serverLink = "http://localhost:5000/cars";

// For online api server link enable this and comment the above serverLink
const serverLink = "https://white-panda-car-rental-v2-api.herokuapp.com/cars";

export default serverLink;
